[
 {
  "name": "M1 -> R2/R4: the hot-tomorrow draw goes back to the global step, freezing the pair",
  "anchor": "    items = [rotOn(T2,_pi(pillar),T2), rotOn(T2,_pi(pillar),T2+1)].filter((v,i,a)=>a.indexOf(v)===i).slice(0,2);",
  "replacement": "    items = [rotOn(R,_pi(pillar),R), rotOn(R,_pi(pillar),R+1)].filter((v,i,a)=>a.indexOf(v)===i).slice(0,2);",
  "gate": "gates/g193_rotation_reach.js"
 },
 {
  "name": "M2 -> R7/R3: the clear-runway draw returns to the V178 first-two-members truncation",
  "anchor": "        : [rotOn(T3,_pi(key),T3), rotOn(T3,_pi(key),T3+1)].filter((v,i,a)=>a.indexOf(v)===i).slice(0,2);",
  "replacement": "        : _pi(key).slice(0,2);",
  "gate": "gates/g193_rotation_reach.js"
 },
 {
  "name": "M3 -> R2: the hot-tomorrow clock is off by one turn",
  "anchor": "    const T2 = Math.floor(R/2);",
  "replacement": "    const T2 = Math.floor(R/2)+1;",
  "gate": "gates/g193_rotation_reach.js"
 },
 {
  "name": "M4 -> R3/R8: the clear-runway clock is derived from the wrong offset",
  "anchor": "      const T3 = Math.floor((R-1)/3);",
  "replacement": "      const T3 = Math.floor((R-2)/3);",
  "gate": "gates/g193_rotation_reach.js"
 },
 {
  "name": "M5 -> R2/R5: rotOn ignores its offset, so both halves of the pair are the same member and the block dedups to one",
  "anchor": "  const rotOn = (clock,arr,n)=>arr[(((clock-1+n)%arr.length)+arr.length)%arr.length];",
  "replacement": "  const rotOn = (clock,arr,n)=>arr[(((clock-1)%arr.length)+arr.length)%arr.length];",
  "gate": "gates/g193_rotation_reach.js"
 },
 {
  "name": "M6 -> R5/R2: the pillar is truncated to its first four members again, undoing the D46 membership the rotation now reaches",
  "anchor": "    : (P[key].items||[]).filter(it=>_auxGearOK(it.name, equip));",
  "replacement": "    : (P[key].items||[]).filter(it=>_auxGearOK(it.name, equip)).slice(0,4);",
  "gate": "gates/g193_rotation_reach.js"
 }
]
