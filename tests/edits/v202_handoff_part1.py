#!/usr/bin/env python3
# V202 handoff fold, part 1 of 2 — documentation only.
# Elements 1 (current-state header + most-recent-work), 5 (section 12), 6 (digest line).
# Anchor-asserted: every anchor count==1 before any write. Literal bytes throughout.
import io, sys

P = "/Users/CanasBangin/Desktop/TheBig6V2/IRON_ASYLUM_HANDOFF_1_1.md"
src = io.open(P, encoding="utf-8").read()
orig = src

reps = []

# ---------------------------------------------------------------- E1a: working-file line
A1 = "- **Working file:** `index.html` — **V201**; `<meta name=\"ia-version\">`=**201**. Built in order after V200. **V201 BUILT D94:"
B1 = ("- **Working file:** `index.html` — **V202**; `<meta name=\"ia-version\">`=**202**. Built in order after V201. "
      "**V202 BUILT the NSW test-goal run path: D100 + D101 + D102 + D105-narrowed + D108 + D111, under D112, the two-guide "
      "hierarchy Mario's second NSW PDF forced mid-build — the entered mile and the entered goal now both reach the pace clock "
      "(they reached 0 of 720 and 0 of 160 before), INT recovery is a 2-2.5× time band instead of a fixed `200m` literal at "
      "0.34 of the doctrine minimum, and the day freeze's touched-set reads all four stores. D105's sequencing limb (E9) was "
      "built and then REVERTED BY RULING, byte-identically, for firing on 0 of 54 blocks.** **V201 BUILT D94:")
reps.append((A1, B1, "E1a working-file line"))

# ---------------------------------------------------------------- E1b: D-code registry
A2 = "highest assigned = D99. Next free = D100.**"
B2 = "highest assigned = D115. Next free = D116.**"
reps.append((A2, B2, "E1b D-code registry"))

# ---------------------------------------------------------------- E1c: Most recent work (V202)
A3 = "- **Most recent work (V201): D94 — the deload pull day keeps the pattern its Main already carries.**"
NEW_MRW = """- **Most recent work (V202): the NSW test-goal run path, and the discovery that there are TWO Physical Training Guides.** Reported: the run path is off doctrine on Mario's live program (seed 24865, 5 train days, event 4 weeks out, 8:15 mile entered). Measure found it worse than reported. The entered mile changed **0 of 720** matched pairs' prescribed paces — every pace re-anchored off `expPaceDefaults`, **9:30/mi hardcoded at `:3580`**. Coach's own read then found the GOAL is not wired either: `:5333` passes three literal `null`s into `arguments[9][10][11]`, so `_targetPace` falls to `initialPace × 0.85` and `agePaceScale` is unreachable — **0 of 160** builds move a pace when only the goal changes, and `_targetPace === 0.85 × expDefault` on **60/60**. Neither number the athlete types reached the pace clock. Week-1 SI shipped **63 s/mi slower than doctrine** and never crossed it. INT recovery was a fixed `200m` literal on **11,880 of 11,880** sessions, delivering **0.685× work time, 0.34 of the doctrine minimum**, while the one line stating the rule correctly was dead (**0 of 11,880**). No long-interval session existed on any of **3,780** builds. And the taper keys to program length, not to the event: **`cfg.raceDate` reaches ZERO engine sites** on this path, so **1,890 of 1,890** programs with the event inside them keep scheduling runs after it, mean 4.52 weeks. **THE SESSION'S PIVOT:** mid-build Mario supplied a second NSW guide and **there are TWO**, both titled Physical Training Guide — the 22-page edition in the repo (LSD/LI/SI) and an 11-page sealswcc.com edition (LSD/**CHI/INT**, Table 6) whose vocabulary **the engine already speaks**. Every ruling issued before that point cited the 22-page one exclusively. Coach ruled **D112**, a three-limb hierarchy: A governs what A states; B fills A's silences; **where a standing app ruling covers the point, the app ruling holds and B is recorded as a disagreement**, overturnable only by contradicting the ruling's stated premise. It proved itself immediately — B's Table 6 runs INT reps to 10, A caps at 8, so the shipped ramp reads B's table through A's ceiling. **What shipped:** **D100** (anchor = the athlete's own `PACE_CHART` row read at the GOAL distance via a new `rowPaceAt`, log-interpolated with a running-maximum guard and clamps at both table bounds; the goal's distance, seconds and `ageBracket` wired into `:5333`; `paceGoalTarget` hoisted so sizer and progression share ONE km conversion) · **D101** (one sourced rate, the unsourced `min(12, ×2.5)` cap gone) · **D102** (the anchor sentence stopped asserting something measurably false, and lost its em-dash) · **D105 as narrowed** (recovery as a **2-2.5× time band**, warm-up from B p8, the four INT notes 10 -> 8) · **D111** (`×0.95` -> **−16 s/mi**, A's 4 s per 400 made literal, **0 s oracle residual**) · **E7/E12** (a goal already within reach **holds at current pace** instead of parking the block slower than the athlete already runs, 114/360) · the **D2b-iii PACE CLOCK re-site** (the injury sentence was silently lost by 105 of 293 cards) · **D108** (the freeze's touched-set reads **all four** stores; a day whose only record was a logged load was rewritable, 7 of 15). **What was reverted BY RULING:** D105's sequencing (E9) was built, then proved to have an **unreachable walk limb — 0 of 54 blocks** have an INT week after the first 8-rep week, because the ramp lands 8 on the LAST interval week by construction. Coach: a rule whose limb fires on 0 of 54 is a gate that cannot trip. Reverted byte-identically (md5 `4055817671ba16da1a992d9c457d4906`); the sequencing carries to D114/D115. GREEN across four phases: **30 gate files, PASS 1481 FAIL 0**, the four new V202 gates all RED on V201 (8/25, 11/12, 5/11, 8/2), **0 of 4 vacuous**, 0 assertion rows dropped; sabotage **26 tripped / 0 survived / 0 NOT-APPLIED / 0 CRASH**, sparse not saturated (12 of 26 redden one gate, 6 redden one row); blast radius **15/15 hunks classified, 0 unclassified, 0 unruled removals**; fuzz **729 configs / 383,837 cells**, self-identity 729/729 on BOTH artifacts before any diff, **9,576 differences, 0 unclassified**, and confinement holds at **0 differences across 223,258 non-pace-goal cells**. `HALF_MANNY` `d4364dd3fa63a3a1` **UNMOVED**, era rows written as REFERENCES. **Mario's live program now:** anchor **8:29/mi** (his 8:15 mile read at 1.5 mi), realistic target **7:44/mi**, W1 interval **8:13/mi** with recovery **4:05 to 5:06**, gap a flat 16 s/mi every week. **Flagged, not fixed:** `tests/gate.sh` grades on the summary line and **never reads a gate's exit code** — `g193_budget_floor` exits 3 meaning "deferred claims, not a full pass" and is graded green; it did not bite (the real run supplies a baseline) and is queued for a tests-only pass. And four digests quoted all session as "must not move" (NRC 5K, base, bike, swim) turned out to be **unverifiable from birth** — no cfg for them is recorded anywhere, so they were never pins; `tests/measure/v196_digest_neutrality.js` (336 configs, 294/336 identical, all 42 drifts `run_pace`) is the reproducible replacement. Issued, ruled and unbuilt: **D103, D104, D106, D107, D109, D110, D113, D114, D115** (§12).

"""
reps.append((A3, NEW_MRW + A3, "E1c most-recent-work V202"))

# ---------------------------------------------------------------- E5: section 12 additions
A4 = "## 12. Open / carried forward\n\n"
SEC12 = """- **D103 — RENAME `CHI` -> Long Interval (coach, V202, RULED, UNBUILT).** Guide B calls the middle session CHI; guide A p16 names the same session by its distance form. D112 puts the naming point in A's hands where A states it, so the athlete-facing string becomes the distance form and `chi` stays the internal key. Pairs with **D107**: `CORE_SPEED_TYPES` must gain `chi` in the same pass or the rename lands on a type the speed-day logic does not recognise.
- **D104 — HARD DAYS HARD, EXTENDED TO THE NSW PATH (coach, V202, RULED, UNBUILT).** The doctrine line already binds NRC long-run days (`nrcLegLiftPlacement`, D36). The NSW path has no equivalent, so an INT or SI day can land beside heavy lower work. **Guide B p8's own separation rule is RECORDED AS A DISAGREEMENT under D112**, not adopted: the app's standing hard-days-hard ruling covers the point, so the app ruling holds and B's differing separation is written down rather than shipped.
- **D106 — THE TAPER KEYS TO THE TEST DATE, NOT TO PROGRAM LENGTH (coach, V202, RULED, UNBUILT). This OVERTURNS D5 / V145's pin.** `cfg.raceDate` reaches **zero engine sites** on the test-goal path, so **1,890 of 1,890** programs with the event inside them keep scheduling runs past it, mean 4.52 weeks. The edit is the READER at `:6704`; **`startDate` does NOT move** (race alignment counts back from the event, it does not slide the start). Measured shape: **GROW 0, 876/1440 shrink.** D5's premise was that no test date existed to count from; it does, so the overturn is by contradicted premise, which is what D112's third limb allows.
- **D107 — `CORE_SPEED_TYPES` GAINS `chi` (coach, V202, RULED, UNBUILT).** The set that decides which sessions count as quality does not contain the middle session at all, which is why a CHI week can be scheduled as if it were easy. One member. Ships with or before D103 and D113.
- **D109 — CARDIO NOTE COPY SWEEP (coach, V202, RULED, UNBUILT).** **22 of 33 run notes carry a mid-sentence dash**, against the standing copy rule. D102 fixed exactly one of them because that one was also asserting something false. The remaining 21 are a copy pass, no engine change, and they should be swept in one edit rather than one at a time as other rulings brush past them.
- **D110 — SWIM MIRRORS D100/D101 (coach, V202, RULED, UNBUILT).** The swim path anchors and progresses by the same two mechanisms the run path just had replaced: a hardcoded default in place of the athlete's own entered time, and an unsourced progression cap. Same shape, same fix, separate build because the swim chart is a different table and needs its own oracle.
- **D113 — THE 3-RUN WEEK BECOMES INT / CHI / long run (coach, V202, RULED, UNBUILT). This OVERTURNS V115.** V115 pinned one quality day in a 3-run week. Under the two-guide read the 3-run week is the guide's own standard week and it carries two quality sessions. Mario GAINS a second quality day. Today **SAT is LSD in 0 of 11 weeks**, which is the measurement that shows the current allocation is not the guide's.
- **D114 — GUIDE B TABLE 6 DRIVES INT REPS AND CHI MINUTES (coach, V202, RULED, UNBUILT, INTERIM).** B's Table 6 is the only ramp either guide states in full, so it is read under D112's second limb, **with A's 8-rep ceiling binding inside it** (B runs to 10). **Explicitly interim: its gate is LICENSED TO REFUSE the build that ships D115** — the segment ladders supersede the table, and a ramp pinned to a superseded source must fail loudly rather than quietly out-rank its replacement. This is the licence-as-predicate form, keyed on an `ia-version` that exists today.
- **D115 — A p13 / p16 SEGMENT LADDERS (coach, V202, RULED, BLOCKED ON A DATA-SHAPE CHANGE).** A's tables prescribe mixed ladders, not one rep length: **17 of 26 p13 rows and 15 of 26 p16 rows** are mixed. `_dose` holds ONE rep length, so the ladders cannot be expressed at all until the dose contract carries a segment LIST. That contract change is the build, and the ladder is the payload; D114 holds the ground until then.
- **POST-V202 TESTS-ONLY PASS (queued, no `ia-version` move).** Two items. (1) **`tests/gate.sh` grades on the summary line and never reads a gate's exit code.** `g193_budget_floor` exits **3** — its own signal for "deferred claims, not a full pass" — and is graded GREEN. It did not bite this session because the real run supplies a baseline and the deferred limb never fired, but a gate that can exit non-zero and score green is a gate that cannot report. (2) **Promote `tests/measure/v196_digest_neutrality.js` to a gate and RETIRE the four unverifiable digests.** The NRC 5K, base, bike and swim digests were quoted all session as "must not move" and are **unverifiable from birth** — no cfg producing any of them is recorded anywhere in the tree, so they were never pins in the sense standing ruling 5 requires. The measure (336 configs, **294/336 identical, all 42 drifts `run_pace`**) is the reproducible replacement and is what should carry the claim.
- **THE DOCTRINE GAP: GUIDE A's 26-WEEK TABLES ARE HAND-TRANSCRIBED FROM SCREENSHOTS (V202, disclosed not fixed).** `doctrine/ptg2020_tables_p13_14_16_17.txt` is not OCR of a page, it is a hand transcription. It is internally clean — **52/52 rows checksum-clean and every segment inside A's own stated menus** — but it is the only doctrine artifact in the tree whose provenance is a person reading an image. D115 depends on it entirely. Any ruling that cites p13/p16 numbers should say it is citing the transcription.

"""
reps.append((A4, A4 + SEC12, "E5 section 12 additions"))

# ---------------------------------------------------------------- E6: digest line
A5 = "- **V201 — D94, a posterior Main suppresses the deload pre-pass"
DIGEST = """- **V202 — the NSW test-goal run path, and the session that found there are TWO Physical Training Guides.** Reported as off doctrine on Mario's live program (seed 24865, 5 train days, event 4 weeks out, 8:15 mile); the measure came back worse than the report in three independent places. The entered mile moved **0 of 720** matched pairs' prescribed paces — every pace re-anchored off `expPaceDefaults`, **9:30/mi hardcoded at `:3580`** — and coach's own read of the seam then found the GOAL unwired too: `:5333` passes three literal `null`s into `arguments[9][10][11]`, so `_targetPace` collapses to `initialPace × 0.85`, `agePaceScale` is unreachable, **0 of 160** builds move a pace when only the goal changes and `_targetPace === 0.85 × expDefault` on **60/60**. **Neither number the athlete types reached the pace clock.** Week-1 SI shipped **63 s/mi slower than doctrine** and never crossed it; INT recovery was a fixed `200m` literal on **11,880 of 11,880** sessions at **0.685× work time, 0.34 of the doctrine minimum**, while the one line stating the rule correctly was dead on **0 of 11,880**; no long-interval session existed on any of **3,780** builds; and the taper keys to program length, so **`cfg.raceDate` reaches ZERO engine sites** and **1,890 of 1,890** programs with the event inside them keep scheduling runs after it, mean 4.52 weeks. **Then the session pivoted mid-build:** Mario supplied a second NSW guide and **there are TWO**, both titled Physical Training Guide — the 22-page edition in the repo (LSD/LI/SI) and an 11-page sealswcc.com edition (LSD/**CHI/INT**, Table 6) **whose vocabulary the engine already speaks**, and every ruling issued before that point had cited the 22-page one exclusively. Coach ruled **D112**, three limbs: A governs what A states, B fills A's silences, and **where a standing app ruling covers the point the app ruling holds and B is recorded as a disagreement**, overturnable only by contradicting that ruling's stated premise. It paid for itself in the same build — B's Table 6 runs INT reps to 10 and A caps at 8, so the shipped ramp reads B's table through A's ceiling. Shipped: **D100** (the anchor becomes the athlete's own `PACE_CHART` row read at the GOAL distance through a new `rowPaceAt`, log-interpolated with a running-maximum guard and clamped at both table bounds; distance, seconds and `ageBracket` wired into `:5333`; `paceGoalTarget` hoisted so the sizer and the progression share ONE km conversion), **D101** (one sourced rate, the unsourced `min(12, ×2.5)` cap gone), **D102** (the anchor sentence stopped asserting something measurably false and lost its em-dash), **D105 as narrowed** (recovery as a **2-2.5× time band**, warm-up from B p8, the four INT notes 10 -> 8), **D111** (`×0.95` -> **−16 s/mi**, A's 4 s per 400 made literal, **0 s oracle residual**), **E7/E12** (a goal already within reach **holds at current pace** rather than parking the block slower than the athlete already runs, 114/360), the **D2b-iii PACE CLOCK re-site** (the injury sentence was being silently lost by 105 of 293 cards), and **D108** (the freeze's touched-set reads **all four** stores — a day whose only record was a logged load was rewritable, 7 of 15). **One thing shipped and was then taken back out by ruling:** D105's sequencing limb (E9) was built, then measured to have an **unreachable walk limb — 0 of 54 blocks** have an INT week after the first 8-rep week, because the ramp lands 8 on the LAST interval week by construction; a limb that fires on 0 of 54 is a gate that cannot trip, so it was reverted **byte-identically** (md5 `4055817671ba16da1a992d9c457d4906`) and the sequencing carried forward to D114/D115. GREEN across four phases: **30 gate files, PASS 1481 FAIL 0**, the four new V202 gates **all RED on V201** (8/25, 11/12, 5/11, 8/2) with **0 of 4 vacuous** and 0 assertion rows dropped; sabotage **26 tripped / 0 survived / 0 NOT-APPLIED / 0 CRASH** and **sparse rather than saturated** (12 of 26 redden exactly one gate, 6 redden exactly one row), which is the shape that says the gates discriminate; blast radius **15/15 hunks classified, 0 unclassified, 0 unruled removals**; fuzz **729 configs / 383,837 cells** with self-identity proved **729/729 on BOTH artifacts before any diff**, **9,576 differences and 0 unclassified**, and confinement holding at **0 differences across 223,258 non-pace-goal cells**. `HALF_MANNY` **UNMOVED** at `d4364dd3fa63a3a1`, era rows written as REFERENCES. Mario's live program now reads anchor **8:29/mi** (his 8:15 mile taken at 1.5 mi), realistic target **7:44/mi**, W1 interval **8:13/mi** with recovery **4:05 to 5:06**, and a flat **16 s/mi** gap every week. **Two things flagged and not fixed:** `tests/gate.sh` **grades on the summary line and never reads a gate's exit code**, so `g193_budget_floor` exiting **3** for "deferred claims, not a full pass" is graded green — it did not bite here because the real run supplies a baseline, and it is queued for a tests-only pass; and the four digests quoted all session as "must not move" (NRC 5K, base, bike, swim) turned out **unverifiable from birth**, with no cfg for any of them recorded anywhere, so they were never pins — `tests/measure/v196_digest_neutrality.js` (336 configs, 294/336 identical, all 42 drifts `run_pace`) is the reproducible replacement. Issued, ruled and unbuilt: **D103, D104, D106** (overturns D5/V145), **D107, D109, D110, D113** (overturns V115), **D114** (interim, its gate licensed to REFUSE the build that ships D115), **D115** (blocked on a dose contract carrying a segment list). §12.

"""
reps.append((A5, DIGEST + A5, "E6 digest line"))

# ---------------------------------------------------------------- assert then write
fail = False
for a, b, name in reps:
    c = src.count(a)
    print("anchor %-34s count=%d" % (name, c))
    if c != 1:
        fail = True
if fail:
    sys.exit("ABORT: anchor count != 1, nothing written")

for a, b, name in reps:
    src = src.replace(a, b, 1)

assert src != orig
io.open(P, "w", encoding="utf-8").write(src)
print("WROTE", P)
