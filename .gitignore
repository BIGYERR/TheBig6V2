# doctrine text is local reference for the coach agent; keep it out of GitHub Pages
doctrine/*.txt
# scratch
.last_diff.txt
node_modules/
__pycache__/
*.pyc
.DS_Store

# session profiling tooling (Mario's, not app or test code)
cc_agent_profile.py
profile.txt
